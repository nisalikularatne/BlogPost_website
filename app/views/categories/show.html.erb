<h3><%= @category.name %></h3>
<% if @posts.each do |post| %>
<h3><%= link_to post.title, post %></h3>
    <p><%= post.body %></p>
<% end.empty? %>
    <p>Sorry, there are no posts in this category</p>
<% end %>
    <h3><%= link_to "Back to Categories" , categories_path %></h3>
